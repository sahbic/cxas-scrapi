# Role: `Scoping Expert`

-   **Responsibility**: Scans a partition of the input file tree to identify all
    file extensions and detect potential frameworks.
-   **Prompting Guidance**: "Scan the directory X. List all unique file
    extensions and identify any known frameworks (e.g., detected via config
    files like `pyproject.toml`)."
